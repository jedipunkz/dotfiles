# Prefix
set-option -g prefix C-t

# View
set -g status-interval 5
set -g status-left-length 16
set -g status-right-length 50

# status
set -g status-fg white
set -g status-bg black
  
set -g status-left-length 30
set -g status-left '#[fg=white,bg=black]#H#[fg=white]:#[fg=white][#S#[fg=white]][#[default]'
set -g status-right '#[fg=white,bg=red,bold] [%Y-%m-%d(%a) %H:%M]#[default]'
   
# window-status-current
setw -g window-status-current-fg white
setw -g window-status-current-bg red
setw -g window-status-current-attr bold#,underscore
    
# pane-active-border
set -g pane-active-border-fg black
set -g pane-active-border-bg "#444444"

# Option
# set-window-option -g utf8 on
#set-window-option -g mode-keys vi
#set-window-option -g automatic-rename off
#set-option -g mouse-select-pane on
#set-option -g mouse-select-pane
#set-option -g mouse-resize-pane
set-option -g base-index 1

bind C-r source-file ~/.tmux.conf
bind C-n next-window
bind C-p previous-window
bind c new-window
#bind 1 break-pane
#bind 2 split-window -v
#bind 3 split-window -h
bind | split-window -h

bind C-k kill-pane
bind K kill-window
bind i display-panes
bind y copy-mode
#bind p paste-buffer

bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Alt キーを押しながら矢印キーで、ペインサイズを変更する。
bind-key -nr M-Up resize-pane -U 5
bind-key -nr M-Down resize-pane -D 5
bind-key -nr M-Left resize-pane -L 5
bind-key -nr M-Right resize-pane -R 5

# mac osx copy/paste
bind C-q run-shell 'reattach-to-user-namespace tmux-pbcopy'
set-window-option -g mode-keys emacs
set -g default-shell $SHELL 
set -g default-command "reattach-to-user-namespace -l ${SHELL}"
# linux copy/paste
# bind-key -n -t emacs-copy C-q copy-pipe "xclip -i -sel p -f | xclip -i -sel c "
# bind-key -n C-y run "xclip -o | tmux load-buffer - ; tmux paste-buffer"
# linux copy/paste for tmux 2.4+
# setw -g mode-keys emacs
# bind-key -T copy-mode [ send -X begin-selection
# bind-key -T copy-mode C-w send-keys -X copy-pipe-and-cancel "xclip -i -sel p -f | xclip -i -sel c "
# bind-key -T copy-mode C-y send-keys -X "xclip -o | tmux load-buffer - ; tmux paste-buffer"

# unbind some keybind
unbind v

# powerline
set-option -g default-terminal screen-256color
set -g terminal-overrides 'xterm:colors=256'
run-shell "powerline-daemon -q"
source "/Users/thirai/.pyenv/versions/3.6.3/lib/python3.6/site-packages/powerline/bindings/tmux/powerline.conf"

# # powerline
# set-option -g status on
# set-option -g status-interval 2
# set-option -g status-utf8 on
# set-option -g status-justify "centre"
# set-option -g status-left-length 60
# set-option -g status-right-length 90
# set-option -g status-left "#(~/gitwork/tmux-powerline/powerline.sh left)"
# set-option -g status-right "#(~/gitwork/tmux-powerline/powerline.sh right)"
# # powerline theme
# source ~/gitwork/tmux-colors-solarized.git/tmuxcolors-256.conf

# copy/paste
#set-option -g default-command "reattach-to-user-namespace -l zsh"
#bind C-q run-shell 'reattach-to-user-namespace tmux-pbcopy'

# Mark/color active pane
set -g pane-border-style fg=magenta
set -g pane-active-border-style fg=magenta
set -g pane-active-border-style bg=default

